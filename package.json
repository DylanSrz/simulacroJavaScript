{
  "name": "simulacrojavascript",
  "version": "1.0.0",
  "description": "Internal projects management SPA with authentication, roles and json-server.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "api": "json-server --watch db.json --port 3001",
    "start": "concurrently \"npm run dev\" \"npm run api\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "json-server": "^0.17.4",
    "vite": "^5.4.0"
  }
}
